#include <bits/stdc++.h>
using namespace std;

int main() {
    cin.tie(0)->sync_with_stdio(0);
    string id;
    cin>>id;
    if(id.length()==13) {
        cout<<"yes";
    } else cout<<"no";
    return 0;
}